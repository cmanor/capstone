package com.example.capstone.service;

import com.example.capstone.model.Instance;

public interface InstanceService {

    void makeStartTimeStamp(Instance instance);
    void makeStopTimeStamp(Instance instance);
}
